import Foundation

// Minimal stub matching the fields ResultHighlightMock.generate(for:) reads.
// In the real Cliff app this is a SwiftData @Model with many more properties.
struct Trial {
    let id: UUID
    let duration: Double
}
